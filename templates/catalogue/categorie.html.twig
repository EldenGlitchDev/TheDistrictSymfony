{% extends 'base.html.twig' %}

{% block title %}Catégories
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row g-0">
			<img src="{{ asset ('img/bg1.jpeg') }}" alt="bandereau" title="bandereau" id="imageBandereau">
		</div>
	</div><br>

	<div class="container">
		<div class="row">

			{% for categorie in categories %}

				<div class="col-sm-12 col-lg-4">
					<a href="{{ path ('app_platcat', {categorie_id: categorie.id} ) }}">
						<img src="{{ asset ('/img/category/' ~ categorie.image) }}" class="animeimage posImage" alt="{{ categorie.libelle }}">
						<div class="row justify-content-center">
							<p class="pCouleurtxt col-7">{{ categorie.libelle }}</p>
						</div>
					</a>
				</div>

			{% endfor %}


		</div>
	</div>


{% endblock %}
